<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="SlevinthHeavenEliteDangerous.Controls.ExoBioControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SlevinthHeavenEliteDangerous.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Information Bar -->
        <local:ExoBioInfoCard x:Name="ExoBioInfoCard" Grid.Row="0"/>

        <!-- Card List (2/3 width) -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <local:ExoBioCardList x:Name="ExoBioCardList" Grid.Column="0"/>

            <!-- Sales History (1/3 width) -->
            <local:ExoBioSalesHistory x:Name="ExoBioSalesHistory" Grid.Column="1"/>
        </Grid>
    </Grid>
</UserControl>
