<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="SlevinthHeavenEliteDangerous.Controls.ExoBioSalesHistory"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewmodels="using:SlevinthHeavenEliteDangerous.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                BorderThickness="1" 
                CornerRadius="8,8,0,0" 
                Padding="12"
                Margin="4,12,12,0">
            <StackPanel Spacing="4">
                <TextBlock Text="Sales History" 
                           FontSize="16" 
                           FontWeight="SemiBold"/>
                <TextBlock Text="{Binding TotalSalesFormatted, Mode=OneWay}" 
                           FontSize="12" 
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
        </Border>

        <!-- Sales List -->
        <ScrollViewer Grid.Row="1" Margin="4,0,12,12">
            <ItemsControl ItemsSource="{Binding Sales, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="viewmodels:ExoBioSaleViewModel">
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                                BorderThickness="1,0,1,1" 
                                Padding="12">
                            <StackPanel Spacing="8">
                                <!-- Sale Summary -->
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Spacing="2">
                                        <TextBlock Text="{x:Bind SaleTimestamp}" 
                                                   FontSize="14" 
                                                   FontWeight="SemiBold"/>
                                        <TextBlock FontSize="12" 
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                            <Run Text="{x:Bind ItemCount}"/>
                                            <Run Text="items"/>
                                        </TextBlock>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Spacing="2" HorizontalAlignment="Right">
                                        <TextBlock Text="{x:Bind TotalEarningsFormatted}" 
                                                   FontSize="14" 
                                                   FontWeight="Bold"
                                                   Foreground="{ThemeResource SystemFillColorSuccessBrush}"
                                                   TextAlignment="Right"/>
                                        <TextBlock FontSize="12" 
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                   TextAlignment="Right">
                                            <Run Text="{x:Bind TotalValueFormatted}"/>
                                            <Run Text=" + " Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                                            <Run Text="{x:Bind TotalBonusFormatted}" Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                                        </TextBlock>
                                    </StackPanel>
                                </Grid>

                                <!-- Sold Items -->
                                <ItemsControl ItemsSource="{x:Bind ItemsSold}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="viewmodels:ExoBioSaleItemViewModel">
                                            <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                                                    CornerRadius="4"
                                                    Padding="8,4"
                                                    Margin="0,2">
                                                <StackPanel Spacing="2">
                                                    <!-- Species name + value/bonus/total -->
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>

                                                        <TextBlock Grid.Column="0"
                                                                   Text="{x:Bind Species_Localised}"
                                                                   FontSize="12"
                                                                   VerticalAlignment="Center"/>

                                                        <TextBlock Grid.Column="1"
                                                                   FontSize="12"
                                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                   VerticalAlignment="Center"
                                                                   Margin="8,0,0,0">
                                                            <Run Text="{x:Bind ValueFormatted}"/>
                                                        </TextBlock>
                                                        <TextBlock Grid.Column="2"
                                                                   FontSize="12"
                                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                   VerticalAlignment="Center"
                                                                   Margin="8,0,0,0">
                                                            <Run Text="{x:Bind BonusFormatted}"/>
                                                        </TextBlock>
                                                        <TextBlock Grid.Column="3"
                                                                   Text="{x:Bind TotalFormatted}"
                                                                   FontSize="14"
                                                                   FontWeight="SemiBold"
                                                                   Foreground="{ThemeResource SystemFillColorSuccessBrush}"
                                                                   VerticalAlignment="Center"
                                                                   Margin="8,0,0,0"/>
                                                    </Grid>

                                                    <!-- System name, distance from Sol, and cluster badge -->
                                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                                        <TextBlock Text="{x:Bind SystemName}"
                                                                   FontSize="11"
                                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                                        <TextBlock Text="{x:Bind DistanceFromSolFormatted}"
                                                                   FontSize="11"
                                                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                                                        <Border Background="{ThemeResource SystemFillColorCautionBackgroundBrush}"
                                                                BorderBrush="{ThemeResource SystemFillColorCautionBrush}"
                                                                BorderThickness="1"
                                                                CornerRadius="3"
                                                                Padding="4,1"
                                                                Visibility="{x:Bind ClusterHighlightVisibility, Mode=OneWay}">
                                                            <TextBlock Text="CLUSTER"
                                                                       FontSize="10"
                                                                       FontWeight="SemiBold"
                                                                       Foreground="{ThemeResource SystemFillColorCautionBrush}"/>
                                                        </Border>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>