<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="SlevinthHeavenEliteDangerous.Controls.ExoBioCardList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:SlevinthHeavenEliteDangerous.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <ItemsRepeater ItemsSource="{Binding ExoBioCards}" Margin="12,0,12,12">
            <ItemsRepeater.Layout>
                <UniformGridLayout MinItemWidth="250" MinItemHeight="250" ItemsStretch="Fill"/>
            </ItemsRepeater.Layout>
            <ItemsRepeater.ItemTemplate>
                <DataTemplate x:DataType="vm:ExoBioCardViewModel">
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="12"
                            Margin="4"
                            Opacity="{x:Bind CardOpacity, Mode=OneWay}">
                        <Grid RowSpacing="8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Title and Scan Type Badge -->
                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="{x:Bind Title}" 
                                           FontSize="16" 
                                           FontWeight="SemiBold"
                                           TextWrapping="Wrap"
                                           VerticalAlignment="Center"/>

                                <Border Grid.Column="1"
                                        Background="{ThemeResource SystemFillColorAttentionBrush}"
                                        CornerRadius="4"
                                        Padding="8,4"
                                        Margin="8,0,0,0"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind ScanType, Mode=OneWay}"
                                               FontSize="10"
                                               FontWeight="SemiBold"/>
                                </Border>
                            </Grid>

                            <!-- Details -->
                            <TextBlock Grid.Row="1" Text="{x:Bind Details}" 
                                           FontSize="12" 
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           TextWrapping="Wrap"/>

                            <!-- Values -->
                            <!-- Sample Value (shown when ScanType is Sample) -->
                            <StackPanel Grid.Row="2" Spacing="4" Visibility="{x:Bind ShowSampleValue, Mode=OneWay}">
                                <Border Background="{ThemeResource SystemFillColorCautionBrush}" 
             CornerRadius="4" 
             Padding="8,4">
                                    <StackPanel Spacing="2">
                                        <TextBlock Text="Sample Value" 
                        FontSize="10" 
                        Foreground="White"/>
                                        <TextBlock Text="{x:Bind SampleValueFormatted, Mode=OneWay}" 
                        FontSize="14" 
                        FontWeight="SemiBold"
                        Foreground="White"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>

                            <!-- Estimated Values (shown when ScanType is Analyse) -->
                            <StackPanel Grid.Row="2" Spacing="4" Visibility="{x:Bind ShowEstimates, Mode=OneWay}">
                                <Border Background="{ThemeResource AccentFillColorDefaultBrush}" 
             CornerRadius="4" 
             Padding="8,4">
                                    <StackPanel Spacing="2">
                                        <TextBlock Text="Estimated Value" 
                        FontSize="10" 
                        Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                                        <TextBlock Text="{x:Bind EstimatedValueFormatted, Mode=OneWay}" 
                        FontSize="14" 
                        FontWeight="SemiBold"
                        Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"/>
                                    </StackPanel>
                                </Border>
                                <Border Background="{ThemeResource SystemFillColorSuccessBrush}" 
             CornerRadius="4" 
             Padding="8,4">
                                    <StackPanel Spacing="2">
                                        <TextBlock Text="First Discovery Bonus" 
                        FontSize="10"
                        Foreground="White"/>
                                        <TextBlock Text="{x:Bind EstimatedBonusFormatted, Mode=OneWay}" 
                        FontSize="14" 
                        FontWeight="SemiBold"
                        Foreground="White"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>
    </ScrollViewer>
</UserControl>
